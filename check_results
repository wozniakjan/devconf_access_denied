#!/bin/bash

function out() {
  echo NAME STATUS
  for i in {1..5}; do
    logs=$(oc logs task$i 2>&1)
    if [[ -z $logs ]]; then
      echo task$i ok
    else
      echo task$i fail 
    fi
  done
}

out | column -t
